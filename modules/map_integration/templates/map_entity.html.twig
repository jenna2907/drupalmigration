{#
/**
 * @file map_entity.html.twig
 * Default theme implementation to present Map entity data.
 *
 * This template is used when viewing Map entity pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_map_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('map_entity') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
